// This file ensures the Notification model has NotificationId
// Check if your Notification model already has this - if so, skip this file
namespace VenueBooking.ViewModels
{
    // No additional view models needed — Review, Notification, SeasonalPricing
    // are already defined in your Models folder.
    // This is a placeholder to confirm the ViewModels namespace compiles.

    // Check needed — if your Notification model uses "Id" instead of "NotificationId", 
    // the Notifications view will need adjustment.
}